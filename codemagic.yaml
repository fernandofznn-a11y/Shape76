workflows:
  ios-release:
    name: Shape 76 iPhone

    environment:
      xcode: latest
      node: 22

    scripts:
      - npm install
      - npm run build
      - npx cap sync ios
      - cd ios/App
      - xcode-project build-ipa

    artifacts:
      - build/ios/ipa/*.ipa