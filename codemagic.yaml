workflows:
  ios-release:
    name: Shape76 iOS

    environment:
      node: 22.0.0

    scripts:
      - npm install
      - npm run build
      - npx cap sync ios
      - cd ios/App
      - xcode-project build-ipa --workspace App.xcworkspace --scheme App

    artifacts:
      - ios/App/build/ios/ipa/*.ipa